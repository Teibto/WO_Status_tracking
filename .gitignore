# ไฟล์ lock ของ Excel ที่เกิดตอนเปิดไฟล์ค้างไว้
~$*

# credential / auth ของ SDF — ห้ามเข้า repo
.env
.env.*
*.p12
*.pem

# โปรไฟล์เบราว์เซอร์ตอน QA (ใหญ่และมี session cookie)
.qa-profiles/

# ของที่เครื่องมือสร้าง
node_modules/
*.log

# npm — โปรเจกต์นี้ไม่มี dependency จึงไม่ต้อง commit lockfile (issue #11 · D4)
package-lock.json

# หน้าตัวอย่าง style ที่ generate ได้เอง (qa/make_theme_preview.js) — issue #18
qa/theme-preview.html
