# Style ของรายงานในตระกูลนี้

เอกสารนี้สำหรับคนที่จะแก้หน้าตารายงานใน repo นี้ หรือจะเอารายงานไปฝังในหน้าอื่น
อ่านแล้วรู้ว่าสีและระยะทุกค่ามาจากไหน แก้ที่ไหน และวันที่ย้ายเข้าเมนูของ report-builder ต้องทำอะไรเพิ่ม

---

## ต้นทางของ style

**canonical คือ repo อื่น ไม่ใช่ที่นี่**

```
Foodstar/Reports/General_report/teibto-report-builder/ui-src/styles/builder.css
```

repo นั้นคือ AdvanceReportBuilder (UI brand *Teibto · Universal Report Engine*) ซึ่งมีรายงาน built-in
27 ใบใช้ token ชุดนี้อยู่แล้ว · รายงานของเราเป็นสมาชิกใหม่ของตระกูลเดียวกัน ไม่ใช่ตระกูลของตัวเอง

ที่ repo นี้ token ถูกยกมาไว้ที่ **ไฟล์เดียว**

```
src/FileCabinet/SuiteScripts/Foodstar/WO_Status_tracking/WOReportTheme.js
```

`npm test` มีด่านเทียบค่าทุก token กับ `builder.css` ให้ · ถ้า clone repo ต้นทางไว้ที่ path ข้างบน
ด่านนี้จะจับได้เมื่อค่าต่างกันแม้ตัวเดียว (ทดลองแล้ว: เปลี่ยน `--pj-primary` เทสตกทันที)
ถ้าไม่มี repo นั้นบนเครื่อง ด่านจะข้ามและ**พิมพ์บอก** ไม่เงียบ

## แก้อะไรที่ไหน

| จะแก้ | แก้ที่ | เงื่อนไข |
|---|---|---|
| ค่าสี · ระยะ · ขนาดตัวอักษร | ห้ามแก้ที่ repo นี้ | ต้องแก้ที่ `builder.css` ต้นทางก่อน แล้วยกค่ามา |
| คลาสคอมโพเนนต์ที่รายงานหลายใบใช้ร่วมกัน | `WOReportTheme.js` | ชื่อคลาสต้องตรงกับ `builder.css` |
| กฎ CSS ที่ใช้ในรายงานใบเดียว | `REPORT_CSS` ในไฟล์ Suitelet ใบนั้น | อ้าง token เท่านั้น ห้ามใส่ hex |

`npm test` บังคับข้อสุดท้ายอยู่ — hex ในโค้ดของ entry Suitelet ทำให้เทสตก (`#fff` ยกเว้นให้
เพราะ `builder.css` เองก็เขียนตรง ๆ) · hex ในคอมเมนต์ไม่นับ เพราะคอมเมนต์ที่เล่าว่า
"เดิมสีนี้ ตอนนี้สีนี้" คือหลักฐานที่ต้องเก็บ

## เรื่องฟอนต์ที่ยังไม่จบ

font stack คือ `-apple-system, BlinkMacSystemFont, 'Segoe UI', 'Sarabun', sans-serif`
ตามต้นทางเป๊ะ · แต่ **Sarabun ไม่ได้ฝังมากับหน้า** — จะได้ Sarabun จริงเฉพาะเมื่อ
เครื่องที่เปิดมีฟอนต์นี้ติดตั้งอยู่

ถ้าเครื่องไม่มี ภาษาไทยจะตกไปที่ `sans-serif` ของเครื่องนั้น ซึ่งคือปัญหาเดิมที่งานนี้ตั้งใจแก้
`builder.css` ต้นทางก็มีข้อจำกัดเดียวกัน จึงถือว่าเสมอกันไว้ก่อน · จะปิดประเด็นนี้จริงต้อง
ตัดสินร่วมกับ repo ต้นทางว่าจะโหลดฟอนต์จากที่ไหน (ฝัง woff2 ใน File Cabinet หรือ CDN
ซึ่งบัญชีต้องต่ออินเทอร์เน็ตออกได้) — ยังไม่ตัดสิน

`qa/theme-preview.html` โหลด Sarabun จาก Google Fonts จึงเห็นผล "แบบที่มีฟอนต์"
ซึ่งอาจดูดีกว่าของจริงบนบัญชี · จุดนี้คือเหตุผลที่หน้า preview ไม่นับเป็นหลักฐาน

## คลาสที่ใช้ร่วมกับ report-builder แล้ว

ชื่อพวกนี้ตรงกับ `builder.css` ทุกตัว จึงไปรับ CSS ของที่นั่นได้เลยเมื่อ markup ของเรา
ไปอยู่ในหน้าเดียวกัน

| คลาส | ใช้ทำอะไร | ใครใช้อยู่ |
|---|---|---|
| `.topbar` · `.breadcrumbs` (`.sep` `.current`) · `.engine-badge` | แถบหัวเรื่อง | ทั้งสองใบ |
| `.content` | กรอบเนื้อหา | ทั้งสองใบ |
| `.kpi-grid` · `.kpi-card accent-*` · `.kpi-label` · `.kpi-value` · `.kpi-meta` | แถบสรุปตัวเลข | WO Cost Trace |
| `.btn` (`primary` `danger` `active`) | ปุ่ม | ทั้งสองใบ |
| `.badge` (`info` `success` `warning` `error` `muted`) | ป้ายสถานะ | theme (พร้อมใช้) |
| `.toolbar` · `.record-count` · `.report-section` · `.section-header` · `.schema-notice` | โครงหน้ารายงาน | theme (พร้อมใช้) |

`WOReportTheme.js` มีตัวช่วยสร้าง markup ให้สองอันคือ `topbar()` และ `kpiCard()` / `kpiGrid()`
ใช้ตัวช่วยดีกว่าเขียน `<div class="kpi-card">` เองทุกที่ เพราะโครงในการ์ดจะไม่หลุดกันเอง

**ตัวช่วยทุกตัวรับสตริงที่ escape มาแล้ว** ไม่ escape ซ้ำให้ · ฝั่งเรียกมี `esc` ของตัวเองอยู่แล้ว

### คลาสที่ยังใช้ชื่อของเราเอง

ตั้งใจไม่เปลี่ยน เพราะเป็นของเฉพาะรายงานนี้และ `builder.css` ไม่มีของเทียบ

`.lvl2` `.lvl3` (รางเจาะลึกชั้น BOM) · `.n` `.tot` `.grand` `.sub` (ตารางต้นทุน) ·
`.pill` `.cp` `.twist` (ตารางสถานะ CP) · `.miss` `.xbar` `.scroll` `.rv`

`.tag` เป็นข้อยกเว้น: หน้าตาเหมือน `.badge.info` เป๊ะ แต่คงชื่อเดิมไว้เพราะ markup ใช้อยู่ 19 จุด

## ลำดับ deploy

`WOReportTheme.js` เป็น dependency ของ entry ทั้งสองใบ

- `suitecloud project:deploy` ยิงทั้งชุดในรอบเดียว ไม่มีปัญหา
- `suitecloud file:upload` ทีละไฟล์ **ต้องอัป `WOReportTheme.js` ก่อน entry**
  ไม่งั้น entry จะพัง `MODULE_DOES_NOT_EXIST` (File Cabinet ไม่มีการอัปหลายไฟล์แบบ atomic)

## โหมดฝังในหน้าอื่น (`&embed=1`)

ทั้งสองใบรับพารามิเตอร์ `embed=1` ด้วยความหมายเดียวกัน คือ**ไม่วาดแถบหัวเรื่องของตัวเอง**
ที่เหลือทำงานเหมือนเดิมทุกอย่าง · มีไว้ให้หน้าที่ฝังเราไว้ไม่มีหัวเรื่องซ้อนสองชั้น

`WOStatusTracking.js` มีพารามิเตอร์นี้มาก่อนแล้ว · `WOCostTrace.js` เพิ่มเข้ามาที่ issue #18
ให้เท่ากัน · ลิงก์เจาะลึกและลิงก์แบ่งหน้าพา `embed` ติดไปด้วย จึงไม่หลุดโหมดกลางทาง

## วันที่จะเอาไปเป็นเมนูของ report-builder จริง

**ยังไม่ทำในตอนนี้** และงานที่เหลือส่วนใหญ่**อยู่ใน repo อีกใบ**
(`builder.html` ที่นั่นเป็นไฟล์ generate จาก `ui-src/` และ CI ฝั่งนั้นตกถ้าสองชั้นไม่ตรงกัน
จึงห้ามแก้ตรง ๆ ต้องแก้ `ui-src/` แล้ว `npm run build:ui`)

### สามทางที่เป็นไปได้

| ทาง | ต้องทำอะไร | ต้นทุน |
|---|---|---|
| **ก. chip ลิงก์ออกไปที่ Suitelet ของเรา** | เพิ่มรายการในเมนู แล้วเปิดรายงานเราด้วย `&embed=1` | ต่ำสุด · รายงานยังเป็นของเรา เจาะลึกทำงานได้ครบ |
| **ข. แปลงเป็น schema entry** | ต้องมีตารางแบนระดับบรรทัดให้ engine ที่นั่น query ได้ | สูง · ดูข้อจำกัดข้างล่าง |
| **ค. โมดูล UI เฉพาะทางในหน้านั้น** | เขียน `ui-src/modules/NN_*.js` ใบใหม่ | สูง · ต้องย้าย logic ทั้งชุด 19 query ไปฝั่งนั้น |

**ทาง ก. เป็นทางที่ควรเริ่ม** และงาน style ที่ทำใน issue #18 คือของที่ทำให้ทาง ก.
ไม่ดูเหมือนหน้าแปลกปลอมเมื่อกดจากเมนู

### ข้อจำกัดของทาง ข. ที่ต้องรู้ก่อนตัดสินใจ

schema ของ report-builder คือ **ตารางเดียว + joins + fields แบน** (`nsTable` · `joins` · `fields`)
รายงานของเราไม่ใช่รูปนั้น: หน้าเจาะลึกยิง 19 query แล้วประกอบเป็นทรง WO > batch > task

`stock-card-by-item` (module 25) ของที่นั่นเป็นตัวอย่างรายงานทรงลำดับชั้นที่ทำได้ **แต่**
มันวางอยู่บน schema ระดับบรรทัด (`inventorymovement`) แล้วใช้ group by
ลำดับชั้นเป็นเรื่องการแสดงผล ไม่ใช่โครงข้อมูล · ถ้าจะเดินทาง ข. ต้องยอมออกแบบ
"ตารางแบนระดับบรรทัดของ WO" ขึ้นมาก่อน ไม่ใช่ยก 19 query ไปทั้งชุด

### สิ่งที่ต้องทำใน repo `teibto-report-builder` (ทางไหนก็ต้องทำ)

1. **category ใหม่** — `ARB_SCHEMA_CATEGORIES` ปัจจุบันมี `finance` `inventory` `sales`
   `purchasing` `hr` `statutory` `custom` · **ไม่มี `manufacturing`** ·
   ต้องเพิ่มทั้งใน list และ i18n key `cat.manufacturing` ทั้ง EN และ TH
   (ไม่เพิ่ม รายงานเราจะตกลงกลุ่ม `custom` ซึ่งมีความหมายว่า "คุณสร้างเอง")
2. **สิทธิ์ — ตั้งต้นเป็นปฏิเสธ** · `customrecord_arb_report_acl` ต้องมีแถวของ report id เรา
   ไม่มีแถว = เปิดไม่ได้ · role Administrator ได้รับอนุญาตเสมอโดยข้อยกเว้น
3. metadata ของ chip — `id` `title` `description` `category` `icon` (รายงานที่นั่นทุกใบมีครบ)

## ตัวอย่างหน้าตาที่เห็นตอนนี้

`qa/theme-preview.html` เป็นหน้าภาพรวมที่ render จากข้อมูลทดสอบชุดเดียวกับที่ `npm test` ใช้
เปิดในเบราว์เซอร์ได้เลย ไม่ต้องต่อ NetSuite · สร้างใหม่ด้วย

```bash
node qa/make_theme_preview.js
```

หน้านี้เป็นของดูเทียบสายตาเท่านั้น ไม่ใช่หลักฐานว่าบน NetSuite ก็ได้แบบนี้
(สิ่งที่ต่างจริงคือฟอนต์ที่บัญชีมีให้ใช้) การยืนยันของจริงต้อง deploy แล้วเปิดดู

---

เอกสารขัดกับความจริงเมื่อไหร่ แก้ที่นี่ในรอบนั้นเลย และอ้าง issue ที่ทำให้ต้องแก้ ·
กติกาการ deploy อยู่ที่ [`README.md`](./README.md)
